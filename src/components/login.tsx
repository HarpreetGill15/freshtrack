import { useState } from 'react'
import { Button } from './ui/button'
export function Login({ onLogin }: { onLogin: (email: string, password: string) => Promise<unknown> }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit(e: React.FormEvent) { e.preventDefault(); setBusy(true); setError(''); try { await onLogin(email, password) } catch { setError('Sign-in failed. Check your email and password.') } finally { setBusy(false) } } return <main className="grid min-h-screen place-items-center bg-brand-50 p-5"><form onSubmit={submit} className="w-full max-w-sm rounded-3xl bg-white p-7 shadow-card"><div className="mb-8"><div className="mb-3 grid h-11 w-11 place-items-center rounded-2xl bg-brand-600 text-xl font-black text-white">F</div><h1 className="text-2xl font-bold text-slate-900">FreshTrack</h1><p className="mt-1 text-sm text-slate-500">Sign in to manage store freshness.</p></div><label className="block text-sm font-medium">Email<input className="mt-1 w-full rounded-xl border border-slate-200 p-3 outline-brand-500" type="email" value={email} onChange={e => setEmail(e.target.value)} required /></label><label className="mt-4 block text-sm font-medium">Password<input className="mt-1 w-full rounded-xl border border-slate-200 p-3 outline-brand-500" type="password" value={password} onChange={e => setPassword(e.target.value)} required /></label>{error && <p className="mt-3 text-sm text-red-600">{error}</p>}<Button className="mt-6 w-full" disabled={busy}>{busy ? 'Signing in…' : 'Sign in'}</Button></form></main> }
